<% content_for :title, "Add Member — Admin" %>
<% content_for :header_title, "Admin" %>

<%= render "admin/nav" %>

<div class="admin-header">
  <h2 class="admin-header__title">Add Member</h2>
</div>

<div class="form-card">
  <%= form_with model: [:admin, @membership] do |f| %>
    <%= render "form", f: f, membership: @membership %>
  <% end %>
</div>
