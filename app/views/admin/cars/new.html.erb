<% content_for :title, "Add Car — Admin" %>
<% content_for :header_title, "Admin" %>

<%= render "admin/nav" %>

<div class="admin-header">
  <h2 class="admin-header__title">Add Car</h2>
</div>

<div class="form-card">
  <%= form_with model: [:admin, @car] do |f| %>
    <%= render "form", f: f, car: @car %>
  <% end %>
</div>
