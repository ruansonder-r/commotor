<% content_for :title, "Edit Trip — Admin" %>
<% content_for :header_title, "Admin" %>

<%= render "admin/nav" %>

<div class="admin-header">
  <h2 class="admin-header__title">Edit Trip</h2>
</div>

<div class="form-card">
  <%= form_with model: [:admin, @trip] do |f| %>
    <%= render "form", f: f, trip: @trip %>
  <% end %>
</div>
